<header class="intro">
    <div class="intro-body ">
        <div class="container text-center ">
            <div class="row">
                <div class="col-md-12 brand-section">
                    <h1 class="brand-heading">Giải mã cuộc đời</h1>
                    <p class="intro-text"><b> "Mảnh ghép hoàn hảo” của tôi đang ở đâu ?</b></p>
                    <%= link_to 'Giải Mã Ngay', new_numerology_path,  {:remote => true, 'data-toggle' =>  "modal", 'data-target' => '#modal-window', class: 'btn btn-info btn-lg'}  %>
                </div>
            </div>
        </div>
    </div>
</header>

<div id="modal-window" class="modal hide fade" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header  text-center">
                <h4 class="modal-title w-100" id="myModalLabel">Giải Mã Thần Số học</h4>
                <button type="button" class="close" data-dismiss="modal" style="margin-right: 10px;">x</button>
                </div>
                <div class="modal-body ">
                <%= bootstrap_form_for(@numerology) do |f| %>
                    <%= f.text_field :name, label: "Họ và tên (Không dấu)", :required => true %>
                    <%= f.text_field :day_of_birth, label: "Ngày sinh", :required => true, class: "datetimepicker", "data-provide": 'datepicker'%>
                    <%= f.text_field :name2, label: "Họ và tên người ấy" %>
                    <%= f.text_field :day_of_birth2, label: "Ngày sinh người ấy", class: "datetimepicker", "data-provide": 'datepicker'%>
                    <%= f.text_field :phone, label: "Điện thoại", :required => true%>
                    <%= f.email_field :email, label: "Email", :required => true%>
                    <div class="text-center">
                        <%= f.submit "Đăng ký", class: 'btn btn-info btn-lg' %>
                    </div>
                    <%# <div class="form-group d-flex-2"> %>
                    <%# <%= render "devise/shared/links" %>
                    <%# </div> %>
                <% end %>
            </div>
        </div>
    </div>
</div>
